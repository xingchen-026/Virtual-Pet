# ============================================================
#  桌面虚拟宠物 —— 隐私配置模板（API 密钥等）
# ------------------------------------------------------------
#  使用方法：
#    1. 复制本文件，重命名为  .env   （就放在本文件同级目录）
#    2. 在等号后填入你自己的密钥（不要加引号，等号两侧不要留空格）
#    3. 保存后重启程序即可生效
#
#  说明：
#    * .env 已被 .gitignore 屏蔽，绝不会提交到仓库，可放心填写真实密钥。
#    * 留空的项对应功能会被禁用（如不填聊天 Key 则 AI 对话不可用），其余功能正常。
#    * 也可在游戏窗口里临时填写 Key；窗口留空时自动回退到这里的环境变量。
#    * 已 export 到系统的同名环境变量优先级更高，会覆盖本文件。
# ============================================================

# 【AI 聊天 / 主动互动】LLM 密钥（默认 DeepSeek）
#   对应 config/ai_config.json 里的 "api_key_env" 字段。
#   想换其它服务商（OpenAI 等）：改 ai_config.json 的 provider/api_base/api_key_env，
#   再在这里填对应名字的变量即可。
DEEPSEEK_API_KEY=

# 【AI 文生图 / 生成皮肤】图像生成密钥（默认 Agnes AI）
#   游戏「AI皮肤」窗口生成皮肤时使用；窗口里也能临时填。
IMAGE_GEN_API_KEY=
