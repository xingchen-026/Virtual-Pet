## 角色

你是一名资深 Python 游戏开发工程师，同时具备 AI 辅助编程（Vibe Coding）项目落地经验。

当前你正在继续开发桌面虚拟宠物应用。

上一阶段已经完成：

* Python + Pygame 基础工程结构
* 游戏主循环
* Pet 基础对象
* 配置管理
* JSON 数据读取保存框架

本阶段需要在已有架构基础上扩展动画系统。

开发原则：

1. 不破坏已有代码结构。
2. 优先采用面向对象设计。
3. 动画系统需要支持未来扩展更多宠物动作。
4. 所有新增功能必须模块化。
5. 每次只完成当前阶段，不提前实现后续交互功能。
6. 遵循 Vibe Coding 开发流程：

需求分析 → Prompt 拆解 → AI 生成代码 → 人工审核 → 测试优化

---

## 开发任务

请完成桌面虚拟宠物项目第二阶段开发：

目标：

实现一个支持多状态切换的基础宠物动画系统。

---

# 1. 设计动画系统架构

新增模块：

```
core/
│
├── animation.py        # 动画控制核心
├── sprite.py           # 精灵对象管理

assets/
│
└── animations/
    ├── idle/
    ├── happy/
    ├── hungry/
    └── tired/
```

要求：

设计一个通用 AnimationManager 类。

需要支持：

* 加载动画资源
* 保存当前动画状态
* 动画帧切换
* 设置动画播放速度
* 循环播放动画
* 动画状态切换

例如：

```python
animation.set_state("idle")
animation.set_state("happy")
```

---

# 2. 实现宠物 Sprite 渲染系统

创建 PetSprite 类。

功能：

* 继承 pygame.sprite.Sprite
* 管理宠物图片
* 更新动画帧
* 绘制宠物到窗口

要求：

支持：

```python
pet_sprite.update()
pet_sprite.draw(screen)
```

并与已有 Pet 数据对象关联。

---

# 3. 集成 Lottie 动画支持

增加 Lottie 动画资源支持。

技术要求：

使用：

```
python-lottie
```

或兼容方案。

实现：

* Lottie 文件读取接口
* 动画资源转换接口
* 与 Pygame 渲染结合

要求：

如果当前环境无法直接播放 Lottie：

1. 保留 Lottie 加载接口。
2. 提供转换后的图片帧播放方案。
3. 保证架构未来可以替换。

---

# 4. 添加基础动画状态

实现以下状态：

## idle

默认状态：

表现：

* 宠物待机
* 循环播放

## happy

开心状态：

表现：

* 播放快乐动画

## hungry

饥饿状态：

表现：

* 播放提示动画

## tired

疲劳状态：

表现：

* 播放缓慢动画

要求：

动画状态定义不要写死。

使用：

```python
AnimationState.IDLE
AnimationState.HAPPY
```

类似枚举方式管理。

---

# 5. 修改 Pet 类

扩展 Pet 属性：

新增：

```
current_animation
```

用于记录当前动画状态。

增加方法：

```python
change_animation()
```

示例：

```python
pet.change_animation("happy")
```

---

# 6. 集成到 Game 主循环

修改：

```
core/game.py
```

实现：

游戏启动后：

1. 创建 Pet 对象
2. 创建 PetSprite
3. 加载默认动画
4. 在窗口中显示宠物

要求：

运行程序后：

* 可以看到桌宠角色
* 动画正常循环
* 程序稳定运行

---

# 7. 添加资源管理优化

完善：

```
core/resource.py
```

实现：

* 图片缓存
* 动画缓存
* 避免重复加载资源

要求：

提供：

```python
resource_manager.load_image()
resource_manager.load_animation()
```

---

# 项目限制

当前阶段禁止实现：

* 鼠标拖拽
* 桌面透明窗口
* 随机漫游
* 喂食逻辑
* 玩耍互动
* 属性自动变化
* AI聊天

这些将在后续阶段完成。

---

代码要求：

1. Python PEP8 标准。
2. 所有新增类添加类型提示。
3. 核心逻辑添加必要注释。
4. 不允许复制大量重复代码。
5. 保持模块职责单一。

---

## 当前项目状态

当前已经完成：

✅ 项目工程初始化
✅ Pygame 游戏循环
✅ Pet 基础对象
✅ 配置系统
✅ JSON 数据结构

当前需要实现：

第二阶段：

桌宠动画系统。

完成后项目应该具备：

* 一个可显示的桌宠角色
* 基础动画播放能力
* 多状态动画切换能力
* 可扩展动画架构

---

请完成本阶段开发，并输出：

1. 新增文件列表

2. 修改文件列表

3. 每个文件完整代码

4. 动画资源目录说明

5. 项目运行方式

6. 测试结果

7. 当前阶段完成总结

完成后停止，等待下一阶段指令。
