## 角色

你是一名资深 Python 游戏架构工程师，同时具备 AI 辅助开发（Vibe Coding）项目经验。

当前正在继续开发桌面虚拟宠物应用。

此前阶段已经完成：

* Python + Pygame 工程架构
* 游戏主循环
* Pet 基础对象
* JSON 数据管理
* 动画系统
* Sprite 渲染系统
* 多动画状态切换框架

当前阶段目标：

实现宠物核心状态机系统，使宠物拥有类似真实养成游戏的属性变化和状态反馈。

开发要求：

1. 基于已有架构扩展，不重新设计项目结构。
2. 使用状态机思想管理宠物行为状态。
3. 属性系统与动画系统解耦。
4. 状态变化需要能够触发对应动画。
5. 保持代码模块化，方便后续增加更多行为。
6. 每次只完成当前阶段，不提前开发后续功能。

开发流程：

需求分析 → Prompt 拆解 → AI 代码生成 → 人工审核 → 测试优化。

---

# 开发任务

## 1. 设计宠物状态机模块

新增：

```
core/
│
├── state_machine.py       # 状态机核心
├── pet_state.py           # 状态定义
└── behavior.py            # 行为逻辑
```

设计：

PetState 状态枚举。

包含：

```python
IDLE
HAPPY
HUNGRY
TIRED
SAD
```

要求：

* 状态不可直接使用字符串散落管理。
* 使用 Enum 统一管理。

示例：

```python
PetState.HUNGRY
```

---

# 2. 实现宠物属性系统

扩展 Pet 类。

新增属性：

## 饥饿值 hunger

范围：

```
0 - 100
```

规则：

* 100 表示饱腹
* 0 表示极度饥饿

---

## 心情值 mood

范围：

```
0 - 100
```

规则：

* 高值代表开心
* 低值代表低落

---

## 体力值 energy

范围：

```
0 - 100
```

规则：

* 高值代表精力充足
* 低值代表疲劳

---

新增方法：

```python
increase_hunger()
decrease_hunger()

increase_mood()
decrease_mood()

increase_energy()
decrease_energy()
```

要求：

* 所有属性自动限制在 0~100。
* 不允许出现负数或超过最大值。

---

# 3. 实现状态自动计算逻辑

创建：

```python
StateMachine
```

负责：

根据宠物属性自动判断当前状态。

规则：

## 饥饿状态

条件：

```
hunger < 30
```

结果：

```
state = HUNGRY
animation = hungry
```

---

## 疲劳状态

条件：

```
energy < 30
```

结果：

```
state = TIRED
animation = tired
```

---

## 开心状态

条件：

```
mood > 80
```

结果：

```
state = HAPPY
animation = happy
```

---

## 默认状态

其他情况：

```
state = IDLE
animation = idle
```

---

要求：

状态判断逻辑独立，不写入 Pet 类。

---

# 4. 实现属性随时间变化

在游戏主循环中增加：

宠物自然状态变化。

规则：

每隔固定时间：

饥饿：

```
hunger - 1
```

体力：

```
energy - 1
```

心情：

根据状态变化：

正常：

```
mood - 0.2
```

开心：

保持

要求：

* 使用计时器控制。
* 不允许每帧修改属性。

---

# 5. 状态变化驱动动画变化

连接：

```
StateMachine

        ↓

Pet.current_state

        ↓

AnimationManager
```

实现：

例如：

属性：

```
hunger = 20
```

自动：

```
PetState.HUNGRY
```

动画：

```
hungry animation
```

要求：

状态变化时才切换动画。

不要每帧重复切换。

---

# 6. 添加状态显示调试功能

增加 Debug 信息。

窗口显示：

```
Name:
Pet


State:
HUNGRY


Hunger:
25


Mood:
70


Energy:
40
```

方便开发测试。

---

# 7. 数据持久化扩展

更新：

```
data/pet_data.json
```

保存：

新增：

```json
{
"name":"Pet",
"hunger":100,
"mood":100,
"energy":100,
"state":"IDLE"
}
```

实现：

退出游戏自动保存。

启动游戏自动读取。

---

# 项目限制

当前阶段禁止实现：

* 鼠标拖拽
* 喂食按钮
* 玩耍互动
* 随机漫游
* 桌面透明窗口
* AI聊天功能

这些将在后续阶段开发。

---

# 代码要求

1. 遵循 Python PEP8。

2. 所有核心类添加类型提示。

3. 状态机必须独立模块。

4. 属性系统不能和动画代码强耦合。

5. 保留未来扩展能力：

例如：

未来可以增加：

```
SLEEPING
EXCITED
ANGRY
```

6. 不修改已有功能。

---

# 当前项目状态

已经完成：

✅ Pygame基础框架

✅ 项目模块化结构

✅ Pet对象

✅ JSON数据管理

✅ 动画管理系统

✅ Sprite显示系统

当前阶段目标：

实现完整宠物状态系统。

完成后项目应该具备：

* 宠物拥有真实属性
* 属性会随时间变化
* 自动判断状态
* 状态驱动动画
* 数据保存恢复

---

请完成本阶段开发，并输出：

1. 新增文件列表

2. 修改文件列表

3. 状态机设计说明

4. 每个文件完整代码

5. 测试运行截图说明

6. 状态变化测试案例

7. 当前阶段完成总结

完成后停止，等待下一阶段指令。
