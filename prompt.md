## 角色

你是一名资深 Python 桌面应用开发工程师，同时具备游戏开发和 AI 辅助开发（Vibe Coding）实践经验。

当前正在继续开发 Virtual-Pet 桌面虚拟宠物项目。

此前阶段已经完成：

* Python + Pygame 游戏框架
* Pet 核心对象
* JSON 数据持久化
* 动画系统
* Sprite 渲染系统
* 状态机系统
* 属性养成系统
* 鼠标交互系统
* 喂食/玩耍行为
* 自主行为系统
* 随机漫游

当前阶段目标：

将当前 Pygame 游戏窗口升级为真正桌面宠物应用。

实现：

* 桌面透明悬浮
* 无边框窗口
* 窗口置顶
* 桌面拖动体验
* 系统托盘管理
* 应用生命周期管理

开发原则：

1. 保持现有游戏逻辑不变。
2. 桌面窗口功能与游戏逻辑解耦。
3. 使用独立 DesktopManager 管理桌面能力。
4. 保证 Windows 平台稳定运行。
5. 不提前开发 AI 对话功能。

开发流程：

需求分析 → Prompt拆解 → AI生成代码 → 人工审核 → 测试优化。

---

# 开发任务

## 1. 创建桌面窗口管理模块

新增：

```
core/

├── desktop.py

```

设计：

DesktopManager 类。

负责：

* 创建桌面窗口
* 设置窗口属性
* 管理窗口状态

接口：

```python
desktop.initialize()

desktop.set_transparent()

desktop.set_topmost()

desktop.hide()

desktop.show()
```

---

# 2. 实现透明桌宠窗口

目标：

运行程序后：

不显示传统游戏窗口。

效果：

类似：

* 桌面漂浮宠物
* 角色悬浮在桌面

要求：

实现：

* 无边框窗口
* 背景透明
* 只显示宠物

支持：

```python
pygame.NOFRAME
```

如果 Pygame 原生能力不足：

允许结合：

* tkinter
* pywin32

但必须保持架构清晰。

---

# 3. 实现窗口置顶

新增：

窗口管理能力。

功能：

启动后：

宠物始终显示在其他窗口上方。

接口：

```python
desktop.keep_on_top()
```

要求：

不能影响：

* 鼠标事件
* 动画刷新
* 性能

---

# 4. 实现桌面拖动优化

当前已有：

宠物拖拽。

升级：

支持：

直接拖动整个桌宠窗口。

效果：

用户拖动宠物：

窗口跟随移动。

要求：

区分：

普通互动拖拽

和

窗口移动拖拽

---

# 5. 添加系统托盘

新增：

```
utils/

tray.py
```

使用：

pystray

实现：

后台运行。

托盘菜单：

```
Virtual Pet

├── 显示宠物
├── 隐藏宠物
├── 保存数据
├── 退出程序
```

---

# 6. 优化程序启动流程

修改：

main.py

启动流程：

```
程序启动

↓

加载配置

↓

加载宠物数据

↓

初始化桌面窗口

↓

初始化动画

↓

启动游戏循环

↓

后台托盘运行
```

---

# 7. 增加应用配置系统

新增：

```
config/

desktop_config.json
```

保存：

```json
{
"transparent":true,
"always_on_top":true,
"window_position":[500,300],
"start_hidden":false
}
```

要求：

禁止硬编码。

---

# 8. 增加异常处理

新增：

```
utils/

exception.py
```

处理：

* 资源不存在
* 动画加载失败
* 存档损坏
* 窗口初始化失败

要求：

程序不能直接崩溃。

---

# 9. 性能优化

检查：

* FPS稳定性
* CPU占用
* 内存增长

优化：

* 动画缓存
* 图片缓存
* 避免重复加载

目标：

后台运行：

CPU < 5%

内存稳定。

---

# 项目限制

当前阶段禁止实现：

* AI聊天
* 语音识别
* 大模型接入
* 多宠物
* 云同步
* 商店系统

这些属于后续增强阶段。

---

# 代码要求

1. 遵循 Python PEP8。

2. 新增模块：

必须职责单一。

禁止：

```python
game.py

里面直接处理Windows窗口API
```

要求：

```
Game

↓

DesktopManager

↓

OS API
```

3. 保持跨平台兼容：

Windows优先。

Linux/Mac保持基础运行。

4. 添加类型提示。

---

# 当前项目状态

已经完成：

✅ 游戏核心

✅ 宠物系统

✅ 动画系统

✅ 行为系统

✅ 交互系统

当前阶段目标：

产品化桌宠应用。

完成后：

项目应达到：

* 像真实桌宠一样漂浮在桌面
* 可以隐藏/显示
* 可以后台运行
* 可以托盘管理
* 用户体验接近商业桌宠软件

---

请完成本阶段开发，并输出：

1. 新增文件列表

2. 修改文件列表

3. 桌面架构设计说明

4. Windows窗口实现方案

5. 每个文件完整代码

6. 启动流程说明

7. 性能测试结果

8. 当前阶段完成总结

完成后停止，等待下一阶段指令。
