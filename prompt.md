## 角色

你是一名资深 Python 游戏开发工程师，同时具备 AI 辅助开发（Vibe Coding）实践经验。

你的任务是协助我使用 Python + Pygame 开发一个桌面虚拟宠物应用。

开发过程中需要遵循以下原则：

1. 采用模块化工程设计，避免所有代码集中在单文件中。
2. 优先设计清晰的项目架构，为后续增加动画系统、宠物状态机、交互逻辑、数据持久化等功能提供扩展能力。
3. 所有代码需要保证可运行、易维护，并添加必要注释。
4. 开发过程采用增量式方式，每完成一个阶段后等待确认，再继续下一阶段。
5. 你需要像真实软件工程团队成员一样，根据需求分析 → 架构设计 → 编码实现 → 测试验证的流程推进。

---

## 开发任务

请完成桌面虚拟宠物项目的第一阶段开发：

### 1. 初始化项目工程结构

创建如下项目目录：

```
VirtualPet/
│
├── main.py                  # 程序入口
│
├── config/
│   └── settings.py           # 全局配置
│
├── core/
│   ├── pet.py                # 宠物核心类
│   ├── game.py               # 游戏主循环
│   └── resource.py           # 资源管理
│
├── assets/
│   ├── images/               # 图片资源
│   ├── animations/           # 动画资源
│   └── sounds/               # 音频资源
│
├── data/
│   └── pet_data.json         # 宠物数据存储
│
├── utils/
│   └── helper.py             # 工具函数
│
└── README.md
```

---

### 2. 搭建 Pygame 基础运行环境

实现：

* 初始化 Pygame
* 创建桌面窗口
* 设置窗口大小
* 设置窗口标题
* 创建游戏主循环
* 支持正常退出程序

要求：

* 程序启动后显示基础窗口
* 窗口关闭不会报错
* 代码结构方便后续加入桌宠透明窗口、置顶显示等功能

---

### 3. 创建基础 Pet 宠物对象

设计一个基础 Pet 类。

初始属性：

```python
name
age
hunger
mood
energy
position
```

要求：

* 提供初始化方法
* 提供属性修改接口
* 保留未来扩展动画状态、行为状态的能力

---

### 4. 添加基础配置管理

将以下内容独立管理：

* 窗口宽度
* 窗口高度
* FPS
* 默认宠物名称
* 默认属性值

禁止：

* 将配置参数硬编码在业务代码中。

---

### 5. 创建基础数据持久化结构

创建 JSON 数据文件：

```json
{
    "name": "Pet",
    "age": 1,
    "hunger": 100,
    "mood": 100,
    "energy": 100
}
```

暂时只需要完成：

* JSON 文件读取
* JSON 文件保存接口

不需要实现复杂存档逻辑。

---

### 6. 添加基础 README

说明：

* 项目介绍
* 技术栈
* 当前完成阶段
* 后续开发计划

---

## 项目限制

1. 技术栈固定：

* Python
* Pygame
* JSON 数据存储

2. 当前阶段不要实现：

* 动画系统
* Lottie 集成
* 鼠标拖拽
* AI 对话
* 复杂状态机
* 游戏数值成长系统

这些功能将在后续阶段逐步加入。

3. 代码要求：

* 遵循 Python PEP8 规范
* 类与函数命名清晰
* 避免重复代码
* 保持模块低耦合

4. 开发方式：

必须采用 Vibe Coding 模式：

需求描述 → Prompt → AI 生成代码 → 人工审核 → 修改优化

每次只完成当前阶段任务，不提前生成后续功能。

---

## 当前项目状态

当前项目为全新空项目。

已确定目标：

开发一个桌面虚拟宠物应用，通过 AI 编程助手完成主要代码生成。

最终目标：

实现一个具有以下能力的桌宠：

* 多状态动画切换
* 桌面自由移动
* 鼠标拖拽互动
* 饥饿 / 心情 / 体力系统
* 喂食、玩耍等交互
* 数据保存与恢复
* 可扩展宠物养成系统

当前阶段目标：

完成项目基础工程搭建，使后续所有功能可以在稳定架构上持续开发。

**请先完成本阶段代码实现，并输出：**

1. 创建的文件列表
2. 每个文件的完整代码
3. 项目运行方式
4. 当前阶段测试结果

完成后停止，等待下一阶段指令。
